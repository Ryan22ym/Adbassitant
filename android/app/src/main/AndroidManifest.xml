<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 读底层网络的真实 DNS 地址：隧道里必须用「真实存在」的 DNS，
         不能拿 tun 的虚拟地址当 DNS，否则所有应用的域名解析都会失败 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14 (API 34) 起前台服务必须声明具体类型，否则启动即崩 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- 通知（Android 13+ 需要用户授予，失败不影响 VPN，只是没有通知栏入口） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false">

        <!-- 授权 Activity：唯一需要用户操作的地方（弹系统 VPN 授权框） -->
        <activity
            android:name=".AuthorizeActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:launchMode="singleTop"
            android:noHistory="true"
            android:theme="@style/Theme.WeakNetVpn.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- VPN 服务。BIND_VPN_SERVICE 由系统持有，只有系统能绑 -->
        <service
            android:name=".WeakNetVpnService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="weaknet traffic shaping" />
        </service>

        <!-- 控制端口宿主：不建隧道，只保证电脑侧随时能发指令进来 -->
        <service
            android:name=".ControlHostService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="weaknet control channel" />
        </service>

    </application>
</manifest>
